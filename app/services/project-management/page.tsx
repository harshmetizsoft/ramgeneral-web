import ProjectManagementClientPage from "./ProjectManagementClientPage";

export const metadata = {
  title: "Lighting Project Management | RAM General Supply",
  description: "End-to-end logistics, scheduling, material procurement, and supervision to guarantee smooth, on-time project completion with zero downtime.",
};

export default function ProjectManagementPage() {
  return <ProjectManagementClientPage />;
}
